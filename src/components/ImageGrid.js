import { onSnapshot } from "firebase/firestore";
import { useEffect, useState } from "react"

export const ImageGrid = (collectionName) => {


    return (
        <div className="img-grid">
            ImageGrid

        </div>
    )
}